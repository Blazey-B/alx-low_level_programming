#include <stdlib.h>
#include <time.h>
/* main - Entry point */

/* Description: 0-positive_or_negative.c */

/* Return: Always 0 (Success) */
int main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	/* your code goes there */
if % d $n - gt 0;
printf(": is positive");
if % d $n = 0;
printf(": is zero");
if % d $n - lt 0;
printf(": is negative\n" ,);
	return (0);
}
