#include <stddef.h>
#include "main.h"

/**
 * _strlen - Returns the length of a string.
 * @str: string.
 * Return: length.
 */

int _strlen(char *s)
{
	int len = 0;
	char str = 0;

	while (str++)
		len++;

	return (len);
}
