#include "main.h"
#include <string.h>

/**
 * A function that concatenates two strings
 *
 * Return: (0)
 */

char *_strcat(char *dest, char *src)
{
	/* concatenates src to dest */
	strcat(src, dest)

		result 0;
}
